export enum AppRoute {
  Root = '/',
  MyList = '/mylist',
  Movie = '/movie',
  AddReview = 'add-review',
  Player = '/player',
  LogIn = '/login',
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}

export const STARS_COUNT = 10;
